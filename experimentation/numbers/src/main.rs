use numbers::{ say_hello };
// use std::vec::Vec;

fn main() {
    // println!("Hello, world!");
    // nothing();
    say_hello();

    numbers::print(10);

}

fn _nothing () -> () {
    println!("nothing!");
}